"use client"
// is a UI library for displaying toast notifications in React applications.
import {Toaster} from "react-hot-toast"

const Toast = () => {
    return(
        <Toaster/>
    )
}

export default Toast